0. What is your name and student ID?
김광호(Kwangho Kim) 2021-13684
1. What is .gitignore? You can write the answer either in Korean or English.
로컬 저장소에서만 사용하고 원격 저장소에는 올리고 싶지 않아 원격 저장소에 올라가지 않도록 관리하는 파일. .gitignore파일들은 git에서 track하지 않는다.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key
안전하게 원격 저장소의 코드에 접근하기 위해서 사용하는 프로토콜이다(Secure Shell의 약자). 서버에 공개키를 등록하고 내가 개인키를 보유하고 있으면 서버에 접근할 때 개인키와 공개키를 대응해서 매치가 되면 접근할 수 있다.
이 방법을 쓰면 따로 로그인을 하지 않아도 안전하게 서버에 접근해서 push, clone 등을 할 수 있다.